# マイクラリモコン 教材ノートブック — Jupyter特有の注意点メモ

対象: `mc_remote_samples/examples/` のデモ用 .ipynb 群（8〜80歳向け）
位置づけ: 全体作業のときにまとめて反映する。今は設計記録として保持。

---

## 1. 「おまじない」の正体 — autoreload（カーネル再起動なしのリフレッシュ）

### 現状

セル冒頭に以下が「おまじない」として置かれている（説明なし）。

```python
%load_ext autoreload
%autoreload 2
```

### 何をしているか

- `param_mc_remote.py` や `axis_flat.py` など、**自作 .py モジュールを編集したとき、
  カーネルを再起動せずに自動で読み直す**ための仕組み。
- ストーリー仕立てのノートブックで、途中で `mc` 接続や定義済み変数を失いたくない
  （＝再起動したくない）ニーズに直接効く。本デモの設計意図と一致。

### 改善方針（全体作業時に反映）

- 8歳向けには「最初に1回だけ実行する魔法」のままでよい。
- 大人/中高生向けに「おまじないの正体」補足セルを後半 or 末尾に1つ追加。
  - 内容: 「.py を書き換えると本来はカーネル再起動が必要。autoreload が自動で
    読み直すので、ストーリーを止めずに編集を試せる」。
- マジックコマンド（`%`）は **Jupyter/IPython でしか動かない**点を明記。
  - `hello.py` など .py にコピーして使う場合は `%` の2行を削除する必要あり。
  - .py 版では autoreload 不要（保存→再実行で読み直される）。
  - → 「ノートブックでだけ必要なおまじない」という位置づけを明示する。

### autoreload の落とし穴（教材に一言入れる価値あり）

- `from module import 名前` で取り込んだものは **autoreload が追従しないことがある**。
- 本ノートは `import param_mc_remote as param`（モジュール参照）と
  `from param_mc_remote import block`（名前参照）が混在。
  - `param.ADRS_MCR` 経由は再読込が効く。
  - `block` に束縛したオブジェクトを差し替える編集は反映が怪しくなる。
- 「定数を編集したのに変わらない」典型原因がこれ。

---

## 2. Jupyter特有のパス問題（最大の地雷）

### 現状

セル[11]の説明「同じ階層にあるからインポートできる」は、
**ノートブックの場所 = カレントディレクトリ(CWD)** という暗黙の前提に立っている。

### 問題

- Jupyter は「.ipynb があるディレクトリ」を起点にしがち。
- VS Code でノートブックを開くと **ワークスペースのルート**が CWD になることがある。
- ここがズレると `import param_mc_remote` / `import axis_flat` が
  突然 `ModuleNotFoundError` になる。
- 利用者の実行環境が VS Code / Jupyter Lab / Google Colab / クラウドでバラバラ
  なため、対象年齢を考えると説明 or 吸収が必須。

### 改善方針（全体作業時に反映）

プロジェクト全体の `mc_constants.py` 自動生成アーキテクチャの
「生成先CWD一本化 + sys.path.insert」方針と地続き。同じ考え方を教材にも適用する。

選択肢（コスト低い順）:

1. **`.vscode/settings.json` を同梱**し `jupyter.notebookFileRoot` /
   `executeInFileDir` 等で **CWD をノートブックの場所に固定**する。
   → 説明コスト最小・最も確実。既存の `.vscode` 同梱方針と噛み合う。
2. 冒頭「おまじない」セルで sys.path にサンプルディレクトリを追加。
   - Jupyter では `__file__` が使えないため `os.getcwd()` ベースになる。
   - 文字列化した `"__file__"` を使う書き方はトリッキーで教材向きではない。
3. Colab / ローカル で出し分け（環境ごとに別セル）。説明コスト高め。

→ 第一候補は **1（.vscode 同梱で CWD 固定）**。

---

## 3. その他、教材に効くJupyter注意点

### sleep() 多用時のブロッキング

- セル[20][22]等で `sleep()` 多用（マイクラの描画を見せるため必須）。
- セル実行中はカーネルがブロックされる。止めるには「停止（■）」ボタン。
- 知らない学習者が「固まった」と勘違い→セル再実行→**mc接続が多重化**する事故。
- 最初の sleep 登場あたりで「長く待つセルは ■ で止められる」を一言添える。

### 実行順序依存の見えない状態

- セル[3]で `PLAYER_NAME` 定義 → セル[5]で使用、という
  **上から順に実行する前提**。
- 学習者がセルを飛ばす/戻ると「変数が無い」「古い mc 接続が残る」など
  初心者に原因が見えない不具合。
- 改善案: 接続セル[5]/[13]に「再接続しても安全」なガードを入れる
  （既存接続があれば閉じて張り直す / PLAYER_NAME 未定義なら分かりやすい
  メッセージ）。ガードは関数に隠して「おまじない」側に寄せれば見た目はシンプルなまま。

---

## まとめ: 全体作業時の着手ポイント

- [ ] 「おまじないの正体」補足セルの文面（8〜80歳向け）を書く
- [ ] パス問題を吸収: `.vscode` 同梱で CWD 固定（第一候補）
- [ ] 接続セルに再接続ガードを入れる
- [ ] sleep / ■停止 の一言注意を追加
- [ ] `%` マジックは Jupyter限定・.py化時は削除、を明示
